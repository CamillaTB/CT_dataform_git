config {
    type: 'incremental',
    schema: 'managed',
    name: 'CT_vendas_incremental',
    tags: ['inicial', 'incremental'],
    dependencies: ['CT_ext_vendas'],
}


SELECT *
FROM ${ref('CT_ext_vendas')} cl


${
    when(incremental(),
    `WHERE pedido_data > (SELECT MAX(pedido_data) FROM ${self()})`)
}
